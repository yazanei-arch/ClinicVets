namespace ClinicVets
{
    /// <summary>Per-screen background decoration while sharing one dark palette.</summary>
    public enum FormBackgroundStyle
    {
        /// <summary>Plain dark gradient only — used for login.</summary>
        LoginMinimal,
        LoginHero,
        RegisterFocus,
        DashboardWorkspace,
        SearchWorkspace
    }
}
